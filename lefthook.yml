pre-commit:
  parallel: true
  commands:
    write-api-server:
      root: "write-api-server/"
      run: .githooks/pre-commit
    read-model-updater:
      root: "read-model-updater/"
      run: .githooks/pre-commit
    read-api-server:
      root: "read-api-server/"
      run: .githooks/pre-commit
